{#
  /**
  * @file
  * Component for a Nav.
  *
  * Variables:
  * - attributes: [array] HTML attributes for the containing element
  *   - id: [string] A valid HTML ID and guaranteed unique.
  *   - role: [string] accessible role
  *   - aria-labelledby: [string] accessible label
  *   - class: [string] any classed added by user or system
  * - configuration: [array] A list of the block's configuration values.
  *   - label: [string] The configured label for the block.
  * - title_prefix: [array] Additional output populated by modules, intended to be displayed in front of the main title tag that appears in the template.
  * - title_suffix: [array] Additional output populated by modules, intended to be displayed after the main title tag that appears in the template.
  * - content:  [array] The content of this block.
  */
#}

{% set heading_id = attributes.id ~ '-label'|clean_id %}

<nav role="navigation" aria-labelledby="{{ heading_id }}"{{- attributes.addClass(classes)|raw -}}>
  {{ title_prefix }}
  <h2 class="visually-hidden"{{- title_attributes.setAttribute('id', heading_id)|raw -}}>{{ configuration.label }}</h2>
  {{ title_suffix }}

  {% block content %}
    {{ content|raw }}
  {% endblock %}

</nav>
