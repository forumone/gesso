{% set classes = [
  'c-hamburger-button',
  modifier_classes ? modifier_classes : ''
]|join(' ')|trim %}

<button {{ add_attributes({class: classes, type: 'button'}) }}>
  <span class="c-hamburger-button__icon">{{ 'Menu'|t }}</span>
</button>
