{{ attach_library('core/components.gesso--form-item') }}
{% set classes = [
  'c-form-item',
  'c-form-item--radios',
]|join(' ')|trim %}

<div {{ add_attributes({ 'class': classes }) }}>{{ children }}</div>
