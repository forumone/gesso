{% set classes = [
  'l-nav',
  modifier_classes ? modifier_classes : '',
]|join(' ')|trim %}

<nav {{ add_attributes({
  'class': classes,
  'aria-label': label,
}) }}>
  {% block content %}
    {{ nav_content }}
  {% endblock %}
</nav>
