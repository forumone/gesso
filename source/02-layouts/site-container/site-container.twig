{% set classes = [
  'l-site-container',
  modifier_classes ? modifier_classes : '',
]|join(' ')|trim %}

<div {{ add_attributes({ 'class': classes }) }}>
  {% block site_content %}
    {{ site_container_content }}
  {% endblock %}
</div>
