{% set classes = [
  'captioned-image',
  modifier_classes ? modifier_classes,
]|join(' ')|trim %}

<figure class="{{ classes }}">
  {{ media }}
  {% if caption %}
    <figcaption>{{ caption }}</figcaption>
  {% endif %}
</figure>
