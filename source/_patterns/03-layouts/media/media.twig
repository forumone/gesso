{% set classes = [
  'l-media',
  modifier_classes ? modifier_classes,
]|join(' ')|trim %}

<div class="{{ classes }}">
  {% if media %}
    <div class="l-media__object">{{ media }}</div>
  {% endif %}
  <div class="l-media__content">{{ content }}</div>
</div>
