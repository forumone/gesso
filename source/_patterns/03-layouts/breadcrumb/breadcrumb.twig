<div class="l-breadcrumb">
  {% if has_constrain %}
    <div class="l-constrain {{ constrain_modifier_classes }}">
  {% endif %}
  {% block content %}
    Breadcrumb Content
  {% endblock %}
  {% if has_constrain %}
    </div>
  {% endif %}
</div>
