<div class="l-breadcrumb">
  {% if has_constrain %}
    <div class="{{ constrain_classes ?: 'l-constrain' }}">
  {% endif %}
  {% block content %}
    Breadcrumb Content
  {% endblock %}
  {% if has_constrain %}
    </div>
  {% endif %}
</div>
