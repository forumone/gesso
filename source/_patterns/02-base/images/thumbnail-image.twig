<img {% if modifier_classes %}class="{{ modifier_classes }}" {% endif %}src="{{ img.thumbnail.src }}" alt="{{ img.thumbnail.alt }}"{% if img.thumbnail.height %} height="{{ img.thumbnail.height }}"{% endif %}{% if img.thumbnail.width %} width="{{ img.thumbnail.width }}"{% endif %}>
