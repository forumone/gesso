{% import '@macros/gesso.macro.twig' as macros %}

<ul class="tags-list">
  {% for item in items %}
    <li class="tags-list__item">
      {{macros.tag(item.url, item.title)}}
    </li>
  {% endfor %}
</ul>
