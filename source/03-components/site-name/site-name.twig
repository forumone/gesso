{% set classes = [
  'c-site-name',
  modifier_classes ? modifier_classes : '',
]|join(' ')|trim %}

<a {{ add_attributes({
  'class': classes,
  'href': url,
  'title': 'Home'|t,
  'rel': 'home'
}) }}>
  <h1 class="c-site-name__text">{{ site_name }}</h1>
</a>
