{% set classes = [
  'c-tab-button',
  modifier_classes ? modifier_classes : '',
]|join(' ') %}

<button
  class="{{ classes }}"
  type="button"
  role="tab"
  aria-controls="{{ unique_id ~ '-panel-' ~ loop.index }}"
>
  {{ label }}
</button>