{% set classes = [
  'c-tab-panel',
  modifier_classes ? modifier_classes : '',
]|join(' ') %}

<div
  class="{{ classes }}"
  role="tabpanel"
  aria-hidden="true"
  id="{{ unique_id ~ '-panel-' ~ loop.index }}"
  tabindex="0"
>
  {{ content }}
</div>