{{ attach_library('gesso/overlay_menu') }}
{% set classes = [
  'c-overlay-menu',
  modifier_classes ? modifier_classes : ''
]|join(' ')|trim %}

{% set id = 'overlay-menu'|unique_id %}

<nav {{ add_attributes({class: classes, id: id}) }}>
  {% include '@components/menu/menu.twig' with {
    'menu_name': menu_name,
    'items': items,
    'menu_class': ['c-overlay-menu__menu'],
    'item_class': ['c-overlay-menu__item'],
    'link_class': ['c-overlay-menu__link'],
  } %}
</nav>
