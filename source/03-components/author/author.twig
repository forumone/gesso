{% set classes = [
  'author',
  modifier_classes ? modifier_classes : '',
]|join(' ')|trim %}

<div {{ add_attributes({ 'class': classes }) }}>
  <a href="{{ url|default('#0') }}">{{ author }}</a>
</div>
