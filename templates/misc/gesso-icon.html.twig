{#
/**
 * @file
 * Template to display a SVG icon.
 *
 * Available variables:
 * - attributes: array of HTML attributes populated by modules, intended to
 *   be added to the main container tag of this template.
 *   - id: A valid HTML ID and guaranteed unique.
 * - icon: inner contents of the SVG, a <use> tag that references an icon
 *   within the sprite.artifact.svg sprite in the Gesso theme.
 * - icon_name: name of the icon to display.
 * - icon_title: screen-reader-friendly title for the icon.
 */
#}

{% include '@components/icon/icon.twig' with {
  icon_name: icon_name,
  icon_title: icon_title,
} %}
