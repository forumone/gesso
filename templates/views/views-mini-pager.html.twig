{#
/**
 * @file
 * Theme override for a view mini-pager.
 *
 * Available variables:
 * - items: List of pager items.
 */
#}

{% if items %}
  {%
    include '@components/navigation/pager/pager--mini/pager--mini.twig' with {
      modifier_classes: "",
      heading: 'Pagination'|t,
      items: items
    } only
  %}
{% endif %}
