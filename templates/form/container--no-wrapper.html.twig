{#
 /**
  * @file
  * Theme override for a container element with no wrapper markup.
  *
  * Add a view's machine name to the array in
  * gesso_theme_suggestions_container_alter() in /includes/views.inc
  * to have the view use this template instead of container.html.twig.
  */
#}
{{ children }}
