{#
/**
 * @file
 * Theme implementation for a region.
 */
#}
{% if content %}
  {% embed 'gesso:breadcrumb' with {
    'attributes': attributes,
    'has_constrain': false,
  } %}

    {% block content %}
      {{ content }}
    {% endblock %}

  {% endembed %}
{% endif %}
