{#
/**
 * @file
 * Theme override to display a region.
 */
#}
{%
  set classes = [
    'region-' ~ region|clean_class,
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    {{ content }}
  </div>
{% endif %}
