{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 */
#}

{% if breadcrumb %}
  {%
    include "@components/navigation/breadcrumb/breadcrumb.twig" with {
      breadcrumb: breadcrumb
    } only
  %}
{% endif %}
